# libtrack-python
A CLI-based Library Management System built using Python and MySQL that supports book management, member registration, borrowing, and return tracking with due dates and fine calculation.
